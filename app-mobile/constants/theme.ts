/** Sombra ligera para tarjetas (alineada con shadow-lg de la web). */
export const cardShadow = {
  shadowColor: "#000",
  shadowOffset: { width: 0, height: 4 },
  shadowOpacity: 0.18,
  shadowRadius: 12,
  elevation: 4,
} as const;
